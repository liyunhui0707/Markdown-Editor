/* Entry point for the self-contained Toast UI Editor browser bundle.
   Built with: npm run build:editor
   Output:     lib/toastui-bundle.js  (committed, load via <script src>) */
import Editor from '@toast-ui/editor';
window.ToastuiEditor = Editor;
